<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Placement Cell | Home</title>
    <link rel="stylesheet" href="css/index.css">
</head>
<body>
    
    <!-- partial -->
    <%- include('_navbar.ejs')  %>

    <div style="width: 70%; margin:40px auto auto auto; ">
        
        <div style="display: flex; justify-content: space-between; align-items: center; margin:10px 0px;">
            <h2>List of Students</h2>
            <a class="add-btn" href="/student/addStudent">+ Add Student</a>
        </div>


        <% if(students.length > 0) {%>
			<div class="mt-3 mb-3">
				<table class="table table-bordered table-striped">
					<thead class="thead-dark">
						<tr>
							<th scope="col">Batch</th>
							<th scope="col">Name</th>
							<th scope="col">Email</th>
							<th scope="col">Contact</th>
							<th scope="col">DSA</th>
							<th scope="col">WEB</th>
							<th scope="col">REACT</th>
							<th scope="col">Interview</th>
							<th scope="col" class="text-end">Action</th>
						</tr>
					</thead>

					<tbody id="myTable">
						<% for(let student of students) {%>
						<tr>
							<td><%=student.batch%></td>
							<td><%=student.name%></td>
							<td><%=student.email%></td>
							<td><%=student.phone%></td>
							<td><%=student.dsa%></td>
							<td><%=student.webd%></td>
							<td><%=student.react%></td>
							<% if(student.interviews.length) {%>
							<td>
								<% for(let company of student.interviews){%>
								<p  id="compnay_name"><%=company.company%></p>
								<%}%>
							</td>
							<%}else{%>
							<td>NONE</td>
							<%}%>
							<td style="text-align: center;">
								<a href="/student/delete/?id=<%=student.id%>" type="button" class="delete-btn">Delete</a>
							</td>
						</tr>
						<%}%>
					</tbody>
				</table>
			</div>
			<%}else{%>
			<div class="w-75 mx-auto mt-5">
				<h2>No Records Found</h2>
			</div>
			<%}%>
    </div>

</body>
</html>